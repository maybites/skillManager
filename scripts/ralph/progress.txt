# Ralph Progress Log
Started: Tue Mar 24 20:28:37 CET 2026
---

## Codebase Patterns
- Typecheck: `uv run pyright src/` (pyright added as dev dep)
- Entry point: `uv run skillmanager` maps to `skillmanager.main:run`
- NiceGUI: run with `host="127.0.0.1"` for localhost-only
- Build system: hatchling, packages = ["src/skillmanager"]

## 2026-03-24 - US-001
- Created src/skillmanager/__init__.py (empty)
- Created src/skillmanager/main.py with run() function launching NiceGUI on localhost
- Added pyright as dev dependency for typecheck
- Files changed: src/skillmanager/__init__.py, src/skillmanager/main.py, pyproject.toml, uv.lock
- **Learnings for future iterations:**
  - pyright was not pre-installed; added via `uv add --dev pyright`
  - NiceGUI app.title must be set before ui.run() for the window title to work
  - `reload=False` prevents the auto-reload dev mode which would be disruptive for a GUI app
---
